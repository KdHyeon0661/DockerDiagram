<Window x:Class="DockerDiagram.Views.ContainerRenameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Container"
        Width="420"
        Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F6F8"
        FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Rename Container" FontSize="18" FontWeight="SemiBold" Foreground="#222"/>
        <TextBlock Grid.Row="1" Text="New Docker container name" Margin="0,12,0,4" Foreground="#555"/>
        <TextBox x:Name="NameBox"
                 Grid.Row="2"
                 Height="30"
                 VerticalContentAlignment="Center"
                 Padding="6"
                 BorderBrush="#C8CDD3"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="OK" Click="OkButton_Click" Width="76" Height="30" Margin="0,0,8,0" Background="#007ACC" Foreground="White" BorderThickness="0"/>
            <Button Content="Cancel" Click="CancelButton_Click" Width="76" Height="30"/>
        </StackPanel>
    </Grid>
</Window>
